[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
app = { path = "../app" }

actix-web = "4"
actix-web-static-files = "4"
static-files = "0.2"
yew = { version = "0.20", features = ["ssr"] }
log = "0.4"
env_logger = "0.10"

[build-dependencies]
static-files = "0.2"